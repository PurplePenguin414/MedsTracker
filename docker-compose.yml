version: "3.8"

services:
  med-tracker:
    build: .
    container_name: med-tracker
    restart: unless-stopped
    ports:
      - "127.0.0.1:3030:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
